;;;====================================
;;; Begin chunk clj-span.varprop
;;;====================================

(defn fac [n]
  (if (= n 0)
    1
    (* n (fac (- n 1)))))

;;;====================================
;;; Begin chunk clj-span.foo
;;;====================================

(defn half-fac [n]
  (/ (fac n) 2))

;;;====================================
;;; End chunk clj-span.foo
;;;====================================


(defn fac-2 [n]
  (* 2 (fac n)))

;;;====================================
;;; End chunk clj-span.varprop
;;;====================================

